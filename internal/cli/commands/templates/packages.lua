-- Homebrew packages declared for PourOver.
-- Use lowercase Homebrew tokens (e.g. "raycast", not "Raycast").
return {
  taps = {
    -- "homebrew/cask-fonts",
  },
  formulae = {
    "git",
  },
  casks = {
    -- "raycast",
    -- "warp",
  },
}
